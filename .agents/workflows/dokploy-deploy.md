---
description: How to wait for Dokploy deployments after pushing code to GitHub
---

# Dokploy Deployment Workflow

> **DEPRECATED**: This workflow has been merged into `push-deploy.md`. Use `/push-deploy` instead.
> 
> See `push-deploy.md` for the full push & deploy workflow including mandatory changelog updates.
